# deps.ps1 - video-titles dependencies
# No external dependencies - just PowerShell + OPENROUTER_API_KEY in .env

Write-Host "  [ok]   video-titles: no extra dependencies required" -ForegroundColor Green
